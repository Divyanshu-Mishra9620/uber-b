services:
  - type: web
    name: uber-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 4000
      - key: JWT_SECRET
        value: m@i_kyU_B@t@U
      - key: DB_CONNECT
        # Set this in Render Dashboard - Environment section
        # Format: mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority
